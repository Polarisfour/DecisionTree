# DecisionTree
This project implements a decision tree classifier from scratch in Python.   It supports two common attribute selection metrics—Gain Ratio and Gini Index—and includes a simple cross-validation routine to evaluate model performance.
